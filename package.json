{
  "name": "dmytro-klochko-one-pager",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/vault-build.mjs && node scripts/names-build.mjs && astro build",
    "preview": "astro preview",
    "vault": "node scripts/vault-build.mjs"
  },
  "dependencies": {
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
