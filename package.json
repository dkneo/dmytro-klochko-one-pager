{
  "name": "dmytro-klochko-one-pager",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/vault-build.mjs && node scripts/map-build.mjs && astro build && node scripts/sitemap-build.mjs",
    "preview": "astro preview",
    "vault": "node scripts/vault-build.mjs"
  },
  "dependencies": {
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
