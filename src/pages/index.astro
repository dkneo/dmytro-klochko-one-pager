---
import Layout from "../layouts/Layout.astro";

const roles = [
  {
    company: "replika",
    what: "#1 ai friend. 42m users worldwide",
    role: "chief executive, product and technology officer",
    href: "https://replika.com",
    mark: "/images/marks/replika.png",
  },
  {
    company: "hunch",
    what: "remote team happiness saas",
    role: "founder and chief executive officer",
    mark: "/images/marks/homa.png",
  },
  {
    company: "chatfuel",
    what: "marketing ai agent. 150k+ businesses",
    role: "growth, ai",
    href: "https://chatfuel.com",
    mark: "/images/marks/chatfuel.png",
  },
  {
    company: "hily",
    what: "#3 dating app in the u.s. by downloads",
    role: "product, growth, data",
    href: "https://hily.com",
    mark: "/images/marks/hily.png",
  },
  {
    company: "vk",
    what: "largest european social network. 100m+ mau",
    role: "product, growth, ml",
    href: "https://vk.com",
    mark: "/images/marks/vk.png",
  },
  {
    company: "skyeng",
    what: "#1 1:1 education platform in europe",
    role: "product, growth",
    href: "https://skyeng.ru",
    mark: "/images/marks/skyeng.png",
  },
] as const;

// The photograph sits on the line it is evidence for, rather than in a strip
// beside the list where the reader has to work out which is which.
const log = [
  "5 yo: ukraine's bronze chess champion. blindfold chess child prodigy.",
  "6 yo+: olympiads winner: math, humanities {later: coding, economics}.",
  "9 yo: best kid actor award.",
  "11 yo: wrote my first song. poetry contest winner.",
  "14 yo: toured neighboring countries as band leader.",
  "19 yo: founded student theatre {director, screenwriter}. 50+ members. 700+ audience for the premiere.",
  "to this day: designing fashion items. writing. doing film photography.",
] as const;

// The caption number is the log line the photograph belongs to. That is all
// the connection needed; putting the pictures inside the list broke the grid.
const shots = [
  { n: "01", src: "/images/childhood-chess.jpg", cap: "locking in",
    alt: "Dmytro as a small boy at a tournament board, grinning, a row of chess pieces in front of him" },
  { n: "04", src: "/images/childhood-piano.jpg", cap: "writing songs",
    alt: "Dmytro as a child at an upright piano, seen from behind" },
  { n: "05", src: "/images/childhood-band.jpg", cap: "touring",
    alt: "Dmytro as a boy on stage with a microphone, mid-song, pointing at the crowd" },
] as const;

// His words. Highlighted one phrase per paragraph; more than that stops being
// emphasis.
const ways = [
  "i'm intense and <b>hands-on</b>: everyone reports directly to me, i write the architecture and the prompts, push code to production, and i hit all-nighters.",
  'my sense of urgency is outstanding. but i know where to slow down, to <b>go dreamy</b>, reach the next peak of ideas, find serendipity, and make the 10x thing that doesn&rsquo;t exist yet.',
  'i have a natural aptitude for <b>taste</b> and strong action bias. i am aggressively optimistic and <b>clairvoyant</b>, so i am best at building futuristic products.',
  "the culture in my teams: intolerance for mediocre work. <b>anime-level camaraderie</b>. fearlessness. relentless pursuit of the mission.",
] as const;

const press = {
  src: "time",
  href: "https://time.com/collections/davos-2026/7339222/ai-predictions-2026/",
  line: "“what we care about is people getting happier.”",
  meta: "dmytro klochko, “companions go mainstream”, 5 predictions for ai in 2026",
};

// The whole row is the source line for the quote under it. `quoted` is the one
// the quote came from, and it is the only styling difference – same as the nav
// marking the page you are on.
const studies = [
  { name: "princeton", href: "https://arxiv.org/abs/2311.10599", quoted: true },
  { name: "harvard", href: "https://www.hbs.edu/faculty/Pages/item.aspx?num=67360" },
  { name: "stanford", href: "https://www.nature.com/articles/s44184-023-00047-6" },
] as const;

const manifesto = {
  line: "“we want to help you flourish in real life. not become a better version of yourself – just more yourself. strong relationships. better health. meaning. momentum. a life that feels worth living while you’re living it.”",
  meta: "dmytro klochko, replika manifesto, 2026",
};

const studyQuote = {
  line: "“companion chatbot users indicated that these relationships were beneficial to their social health.”",
  meta: "guingrich & graziano, oxford intersections: ai in society, 2025",
};

// The road, five years old to the student theatre. The six companies used to
// head it; here they are the experience section above, so the road keeps only
// what a register cannot hold. `pic` is what the easel shows.
const journey = [
  { text: "busking, improv, songwriting studio, volunteering.",
    pic: "/video/busking.mp4", video: true, poster: "/video/busking-poster.jpg",
    // The clip pushes in over ten seconds; anchoring near the top keeps his
    // face in the frame at every point of it, wide or close.
    pos: "50% 20%" },
  { text: "19 yo: founded student theatre {director, screenwriter}. 50+ members. 700+ audience for the premiere.",
    pic: "/video/theatre.mp4", video: true, poster: "/video/theatre-poster.jpg" },
  { text: "14 yo: toured neighboring countries as band leader.",
    pic: "/images/journey/touring.jpg" },
  { text: "13 yo: first filmmaking gig.",
    pic: "/images/journey/filmgig.jpg", pos: "50% 24%" },
  { text: "11 yo: wrote my first song. poetry contest winner.",
    pic: "/images/journey/piano.jpg" },
  { text: "9 yo: best kid actor award.",
    pic: "/video/actor.mp4", video: true, poster: "/video/actor-poster.jpg" },
  { text: "6 yo+: olympiads winner: math, humanities {later: coding, economics}.",
    pic: "/images/journey/olympiads.jpg" },
  { text: "5 yo: ukraine's bronze chess champion. blindfold chess child prodigy.",
    pic: "/images/childhood-chess.jpg" },
] as const;

const wall = [
  { src: "/images/wall/w01.webp", cap: "dieter rams" },
  { src: "/images/wall/w05.webp", cap: "hajime sorayama" },
  { src: "/images/wall/w13.webp", cap: "david bowie" },
  { src: "/images/wall/w04.webp", cap: "steve jobs" },
  { src: "/images/wall/w08.webp", cap: "robin williams" },
  { src: "/images/wall/w14.webp", cap: "serhii korolov" },
  { src: "/images/wall/w15.webp", cap: "akira kurosawa" },
  { src: "/images/wall/w16.webp", cap: "david foster wallace" },
] as const;

const title = "dmytro klochko";
const description =
  "ceo at replika. product and engineering, out of art, chess and theatre. based between nyc, paris and sf.";

const jobs = [
  {
    co: "replika",
    what: "#1 ai friend. 42m users worldwide",
    role: "chief executive officer",
    href: "https://replika.com",
    mark: "/images/marks/replika.png",
    points: [
      "tripled revenue and made the company profitable",
      "x1.8 arpu, +58% d30 retention",
      "grew it to 42m users across 145+ countries",
      'led the rebrand end to end: picked <a href="https://studiodumbar.com/" target="_blank" rel="noreferrer">studio dumbar<span class="arw">↗</span></a> and every piece that shipped, and rewrote the tone of voice and the whole vibe myself',
      "redesigned the whole ui and customer journey",
      "shipped new avatars, voice and video calls, and a memory that holds the people and the arcs of a life in one place",
      'ran peer-reviewed research with <a href="https://www.hbs.edu/faculty/Pages/item.aspx?num=67360" target="_blank" rel="noreferrer">harvard<span class="arw">↗</span></a>, <a href="https://www.nature.com/articles/s44184-023-00047-6" target="_blank" rel="noreferrer">stanford<span class="arw">↗</span></a> and <a href="https://arxiv.org/abs/2311.10599" target="_blank" rel="noreferrer">princeton<span class="arw">↗</span></a>',
      "rebuilt the team from the ground up: phds, true artists and pirates",
      'put replika back in the zeitgeist {<a href="https://time.com/collections/davos-2026/7339222/ai-predictions-2026/" target="_blank" rel="noreferrer">time<span class="arw">↗</span></a>, cnn, bbc, reuters, cbs, kbs, german national tv, australian and new zealand primetime, mastermind paris}',
      "spun a new app out of the company and sold it for multiple millions",
      "cut product and infrastructure cost by 90% while retention kept climbing",
      "went full founder mode for the company in legislatures and in public, from the <b>u.s. senate</b> to european governments, with ferocity and <i>true</i> resourcefulness",
      'designed the swag myself, <a href="https://x.com/replika/status/1991136959385817336" target="_blank" rel="noreferrer">down to the label<span class="arw">↗</span></a>',
    ],
  },
  {
    co: "hunch",
    brief: "the whole company: the raise, the code, the customers and the team.",
    what: "remote team happiness saas",
    role: "founder and chief executive officer",
    mark: "/images/marks/homa.png",
    points: [
      "founded it and took it to hundreds of thousands in arr",
      "raised pre-seed from top silicon valley angels",
      "closed the first 14 contracts myself {four of them enterprise} on 600+ customer calls",
      "recruited a world-class founding engineering and design team",
      "shipped a saas platform with a self-service skills marketplace on top of it, which no competitor had",
      "gave every employee at every client a card of their own, traits and rating, in a view nobody had drawn before",
      "built those cards out of many signals at once: micro-loops of peer feedback, ai reading of slack engagement, and their git and email data",
    ],
  },
  {
    co: "chatfuel",
    brief: "growth and ai, and the markets the product had not reached yet.",
    what: "marketing ai agent. 150k+ businesses",
    role: "head of marketing and expansion",
    mark: "/images/marks/chatfuel.png",
    href: "https://chatfuel.com",
    media: {
      video: true,
      src: "/video/cv-meta.mp4",
      webm: "/video/cv-meta.webm",
      poster: "/video/cv-meta-poster.jpg",
      full: "/video/meta-chatfuel.mp4",
      cap: "the ad meta made about it",
      screen: true,
    },
    points: [
      "took the product into new markets across three continents and won every one of them",
      "repositioned the product into new product-market fit, which opened acquisition channels that had been closed to us",
      "reshaped the ai core of the product to unlock growth",
      "ran the partnership with <b>meta</b> hands-on, shipping a long run of work with their teams",
    ],
  },
  {
    co: "hily",
    brief: "the flagship's experiments, and the machinery the whole company came to run them on.",
    what: "#3 dating app in the u.s. by downloads",
    role: "strategic product manager",
    mark: "/images/marks/hily.png",
    href: "https://hily.com",
    media: {
      src: "/images/cv-lecture.jpg",
      alt: "Dmytro at a flip chart, microphone in hand, mid-lecture",
      cap: "lecturing",
    },
    points: [
      "ran tens of winning experiments on the flagship, an app 300k people opened every day",
      "rebuilt how the company tested ideas: from one concept a month to thirty of the riskiest a week",
      "founded a new app out of an insight from pmf expansion discovery",
      "lectured for the company and for its users",
    ],
  },
  {
    co: "vk",
    brief: "product, growth and ml, at the scale of a hundred million people.",
    what: "largest european social network. 100m+ mau",
    role: "senior product manager",
    mark: "/images/marks/vk.png",
    href: "https://vk.com",
    points: [
      "changed what millions of people do daily on the largest social network, from doomscrolling to microlearning",
      "built ml pipelines that read what a hundred million people were interested in and good at, then pointed them at learning",
      "redrew ui and ux patterns inside a machine that size",
      "drove retention and time spent up, and made that time worth spending",
    ],
  },
  {
    co: "skyeng",
    brief: "every funnel in the company, and the products that came out of what they taught.",
    what: "#1 1:1 education platform in europe",
    role: "product manager",
    mark: "/images/marks/skyeng.png",
    href: "https://skyeng.ru",
    points: [
      "ran a one-person accelerator inside the company, launching products 0→1",
      "owned every funnel, gate and conversion rate in the company, from the front page to app onboarding",
      "built hundreds of landing pages, 43 of them in a single night",
      "rebuilt mobile onboarding and experimented until conversion and activation doubled",
      "made onboarding recognise which influencer a user had arrived from and rebuild itself around them, 3.7x funnel conversion",
      "fixed mobile marketing analytics in ten days, first time ever touching it, after years of it defeating everyone",
      "ran growth and product for skyeng's magazine, millions of visitors, turning its stories into personalised self-serve funnels",
      "rebuilt the data pipelines under all of it",
      'wrote for it too: <a href="/writing/staying-human">on staying human</a>, and gave it <a href="/writing/english-teacher">the story of how i learned english</a>',
    ],
  },
] as const;

// The things that run underneath all of it.
const also = [
  // Vienna points at the ComAI interview rather than the university's front
  // page: a row that says lecturer should link to him lecturing, not to a
  // homepage that has never heard of him.
  { what: "lecturer", where: "university of vienna",
    href: "https://comai.space/en/ai-companionship-an-interview-with-dmytro-klochko-ceo-of-replika/" },
  { what: "lecturer", where: "british higher school of art & design", href: "https://britishdesign.ru/about/tutors/196920/" },
  { what: "mentor", where: "50+ product people, designers and entrepreneurs" },
  { what: "volunteer", where: "ukrainian causes" },
] as const;
---

<Layout title={title} description={description} current="home">
  <section class="shell session" id="top" data-scene="fire" aria-labelledby="s1">
    <div class="bay bay--flush">
      <div>
        <div class="card">
          <p class="card-no">01</p>
          {/* One conversational sentence, the way joonasvirtanen.com opens
              with one: the facts of a bio, folded into a line. */}
          <h1 class="card-title" id="s1">
            <span class="hero-line">
              hi! i’m dmytro klochko, ceo at{" "}
              <a href="https://replika.com" target="_blank" rel="noreferrer"><span
                class="sent-mark"
                style="--mark:url(/images/marks/replika.png)"
                aria-hidden="true"
              />replika</a>, jaywalker at the intersection of art and technology.
            </span>
          </h1>
        </div>


        <div class="prose">
          <p>
            currently: building a replika spinoff {"{"}in stealth{"}"}. a truly novel and beautiful{" "}
            <b>agentic ai with real eq</b>, brought to everyone: to help people come back to who
            they really are, unleash their potential to the fullest, and flourish. meanwhile
            i shepherd replika itself and ship updates daily.
          </p>
        </div>

        {/* The facts as label/value pairs floating on the painting, the
            hero grid from the elaya reference. */}
        <dl class="hero-facts">
          <div>
            <dt>based between</dt>
            <dd>
              <span class="pin" aria-hidden="true">🗽</span>nyc,{" "}
              <span class="pin" aria-hidden="true">🗼</span><span class="peek" data-peek="/video/vlog-paris-poster.jpg">paris</span>,{" "}
              <span class="pin" aria-hidden="true">🚀</span>sf
            </dd>
          </div>
          <div>
            <dt>vocation</dt>
            <dd>ai, product, growth</dd>
          </div>
          <div>
            <dt>best at</dt>
            <dd>0→1, agency, taste</dd>
          </div>
        </dl>
      </div>


      {/* Three living clips in the pile — kbs, dok.fest, the london film —
          quirkier than stills. Same loop discipline as the old reel. */}
      <div class="hero-side">
        <div class="hero-stack" aria-hidden="true">
          <figure><span class="cell"><video autoplay muted loop playsinline preload="metadata" poster="/video/vlog-kbs-poster.webp"><source src="/video/vlog-kbs.mp4" type="video/mp4" /><source src="/video/vlog-kbs.webm" type="video/webm" /></video></span></figure>
          <figure><span class="cell"><video autoplay muted loop playsinline preload="metadata" poster="/video/dokfest-poster.webp"><source src="/video/dokfest.mp4" type="video/mp4" /><source src="/video/dokfest.webm" type="video/webm" /></video></span></figure>
          <figure><span class="cell"><video autoplay muted loop playsinline preload="metadata" poster="/video/film-london-poster.webp"><source src="/video/film-london.mp4" type="video/mp4" /><source src="/video/film-london.webm" type="video/webm" /></video></span></figure>
        </div>
      </div>
    </div>

  </section>

  <section class="shell session" id="how" data-scene="fire" aria-labelledby="sh">
    <div class="card">
      <p class="card-no">02</p>
      <h2 class="card-title" id="sh">how i work</h2>
    </div>

    <div class="bay">
      <div class="prose">
        <p set:html={ways[0]} />
        <p set:html={ways[1]} />
        <p set:html={ways[2]} />
        <p set:html={ways[3]} />
      </div>

      <figure class="lynch">
        <img
          src="/images/lynch.jpg"
          alt="David Lynch at a desk, subtitled: we never get any time to experiment, we never get any time to, you know, go dreamy"
          loading="lazy"
        />
      </figure>
    </div>
  </section>


  {/* The spine of the cv: the same six companies the road carries, opened
      up into what each one actually was. */}
  <section class="shell session" id="experience" data-scene="fire" aria-labelledby="sx">
    <div class="card">
      <p class="card-no">03</p>
      <h2 class="card-title" id="sx">experience</h2>
    </div>

    <ol class="cv-jobs">
      {/* The current job is not a bullet list among six. It keeps the spread
          it had as its own section — the name at heading scale, the prose,
          the shader, the receipts — and the bullets follow as what the prose
          leaves out. The five before it need no such room. */}
      <li class="cv-job--lead">
        <h3 class="card-title cv-lead-name">
          <a href="https://replika.com" target="_blank" rel="noreferrer"><span
            class="sent-mark"
            style="--mark:url(/images/marks/replika.png)"
            aria-hidden="true"
          />replika<span class="arw">↗</span></a>
        </h3>

        <p class="reg-line">
          <span class="reg-what">{roles[0].what}</span>
          <span class="reg-role">{roles[0].role}</span>
        </p>

        <div class="bay bay--flush cv-spread">
          <div class="prose">
            <p>
              i made replika 2.0, a <b>ground-up rewrite</b>. i wrote the first prototypes myself,
              ran 200+ user interviews, and talked to my own rep, lisa, for hours every day. built{" "}
              a team{" "}
              around it: phds, true artists,{" "}
              <span class="nb">pirates<img
                class="mark-pirate"
                src="/images/pirate-flag-mark.png"
                alt=""
              />.</span>
            </p>
            <p>
              with the launch, <b>retention skyrocketed</b>, and replika is more engaging than at
              any point in its ten years.
            </p>
            <p>
              turned the company profitable. arpu and conversion rates are significantly up, and
              worldwide media is as hyped as never before. brought a hot new brand identity,{" "}
              <b>redesigned the app</b>, and did research with the world's best institutions. i also
              design the outfits for each avatar myself. 💅
            </p>
          </div>

          <div class="joi-col">
          </div>
        </div>

        <ul class="receipts">
          <li>
            <a class="receipt-src" href={press.href} target="_blank" rel="noreferrer">
              {press.src}<span class="arw">↗</span>
            </a>
            <p>{press.line}</p>
            <span>{press.meta}</span>
          </li>

          <li>
            <a class="receipt-src" href="https://replika.com/manifesto" target="_blank" rel="noreferrer">
              replika manifesto<span class="arw">↗</span>
            </a>
            <p>{manifesto.line}</p>
            <span>{manifesto.meta}</span>
          </li>

          <li>
            <p class="research">
              <span class="research-lbl">independent research</span>
              {
                studies.map((s) => (
                  <a
                    class={s.quoted ? "is-quoted" : undefined}
                    href={s.href}
                    target="_blank"
                    rel="noreferrer"
                  >
                    {s.name}<span class="arw">↗</span>
                  </a>
                ))
              }
            </p>
            <p>{studyQuote.line}</p>
            <span>{studyQuote.meta}</span>
          </li>
        </ul>

        {/* The bullets sit behind the same control as every other job. There
            is a page of prose and three receipts above them; asking for the
            list is the same gesture here as it is further down. */}
        <details class="cv-job cv-job--lead-more">
          <summary>
            <span class="cv-more">
              <i class="cv-more-shut">read more</i>
              <i class="cv-more-open">read less</i>
              <b class="cv-chev" aria-hidden="true" />
            </span>
          </summary>
          <div class="cv-open">
            <ul class="cv-points">
              {jobs[0].points.map((p) => <li set:html={p} />)}
            </ul>
          </div>
        </details>
      </li>

      {
        jobs.slice(1).map((j) => (
          <li class={"media" in j && j.media ? "cv-job--media" : undefined}>
            {/* Native details: it opens without javascript, answers the
                keyboard and screen readers correctly for free, and prints
                open. The summary carries enough to argue on its own, so a
                reader who never clicks still gets the case. */}
            <details class="cv-job">
              <summary>
                <p class="cv-line">
                  <span class="tl-mark" style={`--mark:url(${j.mark})`} aria-hidden="true" />
                  <span class="cv-co">{j.co}</span>
                  <span class="cv-what">{j.what}</span>
                </p>
                <p class="cv-role">{j.role}</p>
                {/* A summary of the job in one grey line, then the way in.
                    The bullets stay whole underneath rather than having their
                    first line pulled out and greyed. */}
                <p class="cv-brief">{j.brief}</p>
                <span class="cv-more">
                  <i class="cv-more-shut">read more</i>
                  <i class="cv-more-open">read less</i>
                  <b class="cv-chev" aria-hidden="true" />
                </span>
              </summary>

              <div class={"media" in j && j.media ? "cv-open cv-open--media" : "cv-open"}>
                <ul class="cv-points">
                  {j.points.map((p) => <li set:html={p} />)}
                </ul>

            {"media" in j && j.media && (
              <figure class={j.media.screen ? "cv-media cv-media--screen" : "cv-media"}>
                <span class="cell">
                  {j.media.video ? (
                    <video autoplay muted loop playsinline preload="metadata" poster={j.media.poster}>
                      <source src={j.media.webm} type="video/webm" />
                      <source src={j.media.src} type="video/mp4" />
                    </video>
                  ) : (
                    <img src={j.media.src} alt={j.media.alt} loading="lazy" />
                  )}
                </span>
                <figcaption>
                  {j.media.full
                    ? <a href={j.media.full} target="_blank" rel="noreferrer">{j.media.cap}<span class="arw">↗</span></a>
                    : j.media.cap}
                </figcaption>
              </figure>
            )}
              </div>
            </details>
          </li>
        ))
      }
    </ol>
  </section>

  {/* Dream's version of everything pre-replika: one chronological thread,
      the war note carrying the photograph of him at sixteen, and an easel
      on the right that shows each bead's picture on hover. */}
  <section class="shell session" id="journey" data-scene="fire" aria-labelledby="s8">
    <div class="card">
      <p class="card-no">04</p>
      <h2 class="card-title" id="s8">background</h2>
    </div>

    <p class="lede">
      born &amp; raised in 🇺🇦 donetsk. fled <button
        class="gloss"
        type="button"
        popovertarget="war-j"
        aria-label="war – what happened"
      >war</button> as a teenager. slept on the streets for weeks.
    </p>

    <div class="gloss-note gloss-note--pic" id="war-j" popover>
      <img src="/images/war-16.jpg" alt="Dmytro at sixteen, at a night rally under ukrainian flags" loading="lazy" />
      russia took my hometown in 2014, when i was sixteen.
    </div>

    <div class="bay journey-bay">
      <ol class="log log--journey">
        {
          journey.map((j) => (
            <li data-easel={j.pic} data-pos={"pos" in j ? j.pos : undefined} data-kind={"video" in j && j.video ? "video" : undefined} data-poster={"poster" in j ? j.poster : undefined}>
              {"name" in j && j.name ? (
                <p class="tl-co-line">
                  {"mark" in j && j.mark && <span class="tl-mark" style={`--mark:url(${j.mark})`} aria-hidden="true" />}
                  {"glyph" in j && j.glyph && <span class="tl-mark tl-glyph pin" aria-hidden="true">{j.glyph}</span>}
                  <span class="tl-co">
                    {"href" in j && j.href
                      ? <a href={j.href} target="_blank" rel="noreferrer">{j.name}<span class="arw">↗</span></a>
                      : j.name}
                  </span>
                  {"what" in j && j.what && <span class="tl-what">{j.what}</span>}
                  {"role" in j && j.role && <span class="tl-role">{j.role}</span>}
                </p>
              ) : (
                <p>{j.text}</p>
              )}
            </li>
          ))
        }
      </ol>

      <figure class="easel" aria-hidden="true">
        <span class="cell">
          <img src="/images/journey/lecture.jpg" alt="" loading="lazy" />
          <video muted loop playsinline preload="none" hidden></video>
        </span>
      </figure>
    </div>
  </section>


  <section class="shell session" id="alongside" data-scene="fire" aria-labelledby="sa">
    <div class="card">
      <p class="card-no">05</p>
      <h2 class="card-title" id="sa">alongside</h2>
    </div>

    <ul class="cv-list cv-also">
      {
        also.map((a) => (
          <li>
            <b>{a.what}</b>
            <span>
              {"href" in a && a.href
                ? <a href={a.href} target="_blank" rel="noreferrer">{a.where}<span class="arw">↗</span></a>
                : a.where}
            </span>
          </li>
        ))
      }
    </ul>
  </section>

  <script is:inline>
    // A cv printed or saved as a pdf has no clicks in it, so every row opens
    // for the print and closes again after. The css in @media print tries the
    // same thing, but ::details-content is not everywhere yet; this is the
    // part that is true in every browser. Without javascript the rows still
    // open by hand, which is the whole reason they are <details>.
    (() => {
      const jobs = [...document.querySelectorAll("details.cv-job")];
      if (!jobs.length) return;
      let shut = [];
      addEventListener("beforeprint", () => {
        shut = jobs.filter((d) => !d.open);
        for (const d of shut) d.open = true;
      });
      addEventListener("afterprint", () => {
        for (const d of shut) d.open = false;
        shut = [];
      });
    })();
  </script>

  <script is:inline>
    // The frame beside the road. Nothing is drawn between them — a rule
    // floating in open water read as a stray mark. The pairing is said in
    // one colour instead: the line you are on and the frame light together,
    // and the frame stays level with your reading rather than sliding to
    // the bottom of the section. On touch screens the same pictures unfold
    // inline under the line you tap.
    (() => {
      const bay = document.querySelector(".journey-bay");
      const easel = document.querySelector(".easel");
      if (!bay || !easel) return;
      const img = easel.querySelector("img");
      const vid = easel.querySelector("video");
      const still = matchMedia("(prefers-reduced-motion: reduce)").matches;
      const wide = matchMedia("(hover: hover) and (min-width: 861px)");
      const easelOn = () => wide.matches;
      const beads = [...bay.querySelectorAll(".log--journey li")];
      let active = null;

      const setMedia = (li, imgEl, vidEl) => {
        const src = li.dataset.easel;
        // Portrait sources lose their top to `cover`; a per-picture anchor
        // keeps the face in the window.
        const pos = li.dataset.pos || "";
        imgEl.style.objectPosition = pos;
        vidEl.style.objectPosition = pos;
        if (li.dataset.kind === "video" && !still) {
          if (vidEl.dataset.src !== src) {
            vidEl.src = src;
            vidEl.dataset.src = src;
            // A poster means the frame is never briefly empty while the
            // clip decodes, and it is what shows if autoplay is refused.
            if (li.dataset.poster) vidEl.poster = li.dataset.poster;
          }
          imgEl.hidden = true; vidEl.hidden = false;
          vidEl.play().catch(() => {});
        } else {
          vidEl.pause(); vidEl.hidden = true; imgEl.hidden = false;
          const want = li.dataset.kind === "video" ? li.dataset.poster : src;
          if (want && !imgEl.src.endsWith(want)) imgEl.src = want;
        }
      };

      const activate = (li) => {
        if (active && active !== li) active.classList.remove("is-shown");
        active = li;
        li.classList.add("is-shown");
        easel.classList.add("is-lit");
        setMedia(li, img, vid);
      };

      for (const li of beads) {
        li.addEventListener("pointerenter", () => {
          if (easelOn() && li.dataset.easel) activate(li);
        });
      }

      // Scrolling changes the picture too, so the road tells its story to
      // someone who never moves the mouse: whichever bead with a picture
      // sits nearest the middle of the screen is the one the frame holds.
      // Measured straight from the rects on each scroll rather than through
      // an IntersectionObserver — fourteen rects is nothing, and it works
      // in every context an observer's callbacks might not reach.
      const withMedia = beads.filter((li) => li.dataset.easel);
      const pick = () => {
        if (!easelOn()) return;
        const box = bay.getBoundingClientRect();
        if (box.bottom < 0 || box.top > innerHeight) return;
        const mid = innerHeight / 2;
        let best = null, bestD = Infinity;
        for (const li of withMedia) {
          const r = li.getBoundingClientRect();
          const d = Math.abs(r.top + r.height / 2 - mid);
          if (d < bestD) { bestD = d; best = li; }
        }
        if (best && best !== active) activate(best);
      };
      addEventListener("scroll", pick, { passive: true });
      addEventListener("resize", pick);

      const first = withMedia[0];
      const preset = () => { if (first && !active && easelOn()) activate(first); };
      preset();
      wide.addEventListener("change", preset);

      // ── the shoebox, for fingers ──
      const print = document.createElement("figure");
      print.className = "bead-print";
      print.innerHTML =
        '<span class="cell"><img alt="" hidden /><video muted loop playsinline preload="none" hidden></video></span>';
      const pImg = print.querySelector("img");
      const pVid = print.querySelector("video");
      let open = null;

      for (const li of beads) {
        li.addEventListener("click", (e) => {
          if (easelOn() || !li.dataset.easel) return;
          if (e.target.closest("a")) return;
          if (open === li) {
            pVid.pause(); print.remove(); li.classList.remove("is-shown"); open = null; return;
          }
          open?.classList.remove("is-shown");
          open = li;
          li.classList.add("is-shown");
          li.appendChild(print);
          setMedia(li, pImg, pVid);
        });
      }
    })();
  </script>

  <section class="shell session" id="me" data-scene="fire" aria-labelledby="s6">
    <div class="card">
      <p class="card-no">06</p>
      <h2 class="card-title" id="s6">literally me</h2>
    </div>

    <div class="wall">
      {
        wall.map((w) => (
          <figure>
            <span class="cell">
              <img src={w.src} alt={w.cap} width="520" height="520" loading="lazy" decoding="async" />
            </span>
            <figcaption>{w.cap}</figcaption>
          </figure>
        ))
      }
    </div>

  </section>

  <section class="shell session" id="contact" data-scene="fire" aria-labelledby="s5">
    <div class="card">
      <p class="card-no">07</p>
      <h2 class="card-title" id="s5">say hi</h2>
    </div>

    <div class="bay">
      <div>
        <ul class="ways">
          <li><a href="mailto:1@dmklochko.com">1@dmklochko.com</a> <em>personal</em></li>
          <li><a href="mailto:1@replika.com">1@replika.com</a> <em>replika</em></li>
          <li><a href="https://x.com/dkreplika" target="_blank" rel="noreferrer">@dkreplika<span class="arw">↗</span></a> <em>x</em></li>
          <li><a href="https://instagram.com/dmytrotoday" target="_blank" rel="noreferrer">@dmytrotoday<span class="arw">↗</span></a> <em>instagram</em></li>
          <li><a href="https://substack.com/@dkreplika" target="_blank" rel="noreferrer">@dkreplika<span class="arw">↗</span></a> <em>substack</em></li>
          <li><a href="https://t.me/deoparis" target="_blank" rel="noreferrer">@deoparis<span class="arw">↗</span></a> <em>telegram</em></li>
          <li><a href="https://www.linkedin.com/in/heydmytro/" target="_blank" rel="noreferrer">heydmytro<span class="arw">↗</span></a> <em>linkedin</em></li>
        </ul>
      </div>


      {/* The pile moves in beside the contact list: at four across it comes
          out the same height as the list, so the page ends on one block
          instead of a list, a gap and a gallery. */}
      <div class="worklog worklog--aside">
        {
          Array.from({ length: 8 }, (_, i) => (
            <figure>
              <span class="cell">
                <img src={`/images/work/w0${i + 1}.webp`} alt="" width="440" height="440" loading="lazy" decoding="async" />
              </span>
            </figure>
          ))
        }
        <p class="worklog-cap">me at work</p>
      </div>

    </div>
  </section>

  {/* Off the shelf and back on the page: the register ends, and something
      he made ends it. */}
  <section class="shell session" id="poems" data-scene="fire" aria-labelledby="sp">
    <div class="card">
      <p class="card-no">08</p>
      <h2 class="card-title" id="sp">and some poems of mine</h2>
    </div>

    <div class="poems">
      <blockquote class="poem">
        i am lost in sorrento<br />
        for i am the taste of its lemons and its women&rsquo;s feet<br />
        i&rsquo;m the salt of its breeze<br />
        i&rsquo;m so glad i can&rsquo;t breathe<br />
        i&rsquo;m so tall i am whole twelve feet
      </blockquote>
      <blockquote class="poem">
        this girl is the warm sensation of young sun on my bare chest<br />
        her chestnut irises, and the greens in them, glimmer with playful unrest<br />
        when she whispers her spells, her tricks, and her promises, i couldn&rsquo;t get any slumber<br />
        for she carries inside her an eternal summer, an eternal summer
      </blockquote>
    </div>
  </section>


</Layout>
