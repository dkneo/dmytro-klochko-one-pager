Read `AGENTS.md` in this directory first. It holds the architecture, the
deploy pipeline and its traps, the content that lives in KV rather than git,
the three separate password doors, his taste rules, and the verification
lore for this environment.

Then `CHECKLIST.md` for taste, `DESIGN.md` for tokens, `CURATION.md` for the
vault's rules.
