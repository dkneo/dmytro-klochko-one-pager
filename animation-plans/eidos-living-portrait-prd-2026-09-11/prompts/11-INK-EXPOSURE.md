# Optional material-only Fal prompt

Create a single monochrome dry-ink pressure texture on transparent background. It should resemble uneven relief printing: granular ink, broken pressure, tiny paper show-through and rough natural edges. No subject, character, artwork, object, text, symbol, frame, paper background, color gradient or lighting effect. The texture must tile poorly enough to feel physical but contain no recognizable form.

This output is optional. It is composited inside an exact source mask and never controls geometry.
